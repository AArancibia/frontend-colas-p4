/*
import {Ticket} from '@app/core/models/ticket.model';
import * as Store from '@app/store/colas-store.module';

export interface TicketState { //extends EntityState< Ticket >
  loading: boolean;
  loaded: boolean;
  tickets: Ticket[];
}
*/
